# Contact form backend (Formspree / FormSubmit / Basin / similar POST endpoint).
# The site builds and deploys fine without this — the form shows a "not configured" note.
PUBLIC_FORM_ENDPOINT=

# Optional: Cloudflare Turnstile site key for spam protection on the contact form.
PUBLIC_TURNSTILE_SITE_KEY=
