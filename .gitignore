# build output
dist/
.astro/

# dependencies
node_modules/

# environment
.env
.env.*
!.env.example

# generated migration data at repo root (recreate with: npm run extract)
# note: root-anchored so src/data/ stays committed
/data/

# original WordPress backups — keep locally, do not push to GitHub
# (root-anchored: public/wp-content/uploads/ MUST be committed)
*.sql
*.xml
!public/**/*.xml
/uploads/
/Logo.webp

# macOS
.DS_Store

# editor
.vscode/
.idea/

# vercel
.vercel/
